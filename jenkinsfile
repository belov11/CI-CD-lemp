pipeline {
    agent any
    stages {
        stage('Started ansible playbook') {
            steps {            
                sh '''
                    ansible --version'
                '''
            }
        }
    }
}
