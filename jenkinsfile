pipeline {
    agent any
    environment {
        ANSIBLE_PRIVATE_KEY=credentials('github-ssh-key')
    }
    stages {
        stage('Started ansible playbook') {
            steps {            
                sh 'ansible --version'
                sh 'ansible-playbook -i /etc/ansible/hosts --private-key=$ANSIBLE_PRIVATE_KEY /etc/ansible/docker-install.yml'
            }
        }
    }
}
