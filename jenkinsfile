pipeline {
    agent any
    environment {
        ANSIBLE_PRIVATE_KEY=credentials('jenkins-server')
    }
    stages {
        stage('Started ansible playbook') {
            steps {            
                sh '''
                    ansible --version'
                '''
            }
        }
    }
}
